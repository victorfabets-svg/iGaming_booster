name: Runtime Validation

on:
  workflow_dispatch: {}
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

# GLOBAL ENV CONTRACT — SSOT
env:
  NEON_DB_URL: ${{ secrets.NEON_DB_URL }}
  PORT: 3000
  HOST: 0.0.0.0

jobs:
  db-connection-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install API dependencies
        working-directory: apps/api
        run: npm install

      - name: Test database connection
        working-directory: apps/api
        run: |
          for i in {1..10}; do
            node -e "
            const { Client } = require('pg');
            const client = new Client({ connectionString: process.env.NEON_DB_URL });
            client.connect()
              .then(() => { console.log('DB Connected'); return client.query('SELECT 1'); })
              .then(() => { console.log('Query OK'); client.end(); process.exit(0); })
              .catch(e => { console.error('Error:', e.message); process.exit(1); });
            " && break
            echo "Attempt $i failed, retrying..."
            sleep 2
          done || (echo "All retries failed" && exit 1)

  db-write-test:
    needs: []
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install API dependencies
        working-directory: apps/api
        run: npm install

      - name: Run write test
        working-directory: apps/api
        run: node scripts/db-write-test.js

  runtime-check:
    needs: [db-connection-check, db-write-test]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Validate ENV contract
        run: |
          test -n "$NEON_DB_URL" || (echo "NEON_DB_URL missing" && exit 1)
          test -n "$PORT" || (echo "PORT missing" && exit 1)
          echo "ENV CONTRACT OK"

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install API dependencies
        working-directory: apps/api
        run: npm install

      - name: Runtime check (DB only)
        working-directory: apps/api
        run: node scripts/runtime-check.js

      - name: Build API
        working-directory: apps/api
        run: npm run build

      - name: Upload build
        uses: actions/upload-artifact@v4
        with:
          name: api-build
          path: apps/api/dist/server.js

  pipeline-e2e-test:
    runs-on: ubuntu-latest
    needs: runtime-check

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install API dependencies
        working-directory: apps/api
        run: npm install

      - name: Download build
        uses: actions/download-artifact@v4
        with:
          name: api-build
          path: apps/api/dist

      - name: Verify artifact integrity
        run: |
          test -f apps/api/dist/server.js || (echo "Artifact missing: server.js" && exit 1)
          echo "ARTIFACT OK: $(ls -lh apps/api/dist/server.js)"

      - name: Start API
        working-directory: apps/api
        run: |
          node dist/server.js > api.log 2>&1 &
          echo $! > api.pid
          sleep 2
          echo "=== SERVER LOG ==="
          cat api.log || true

      - name: Debug port
        run: |
          echo "Using PORT=$PORT"

      - name: Check port binding
        run: |
          echo "=== PORT CHECK ==="
          lsof -i :$PORT || true

      - name: Test raw curl
        run: |
          curl -v http://localhost:$PORT/health/db || true

      - name: Wait for API
        run: |
          curl -f --retry 5 --retry-delay 1 http://localhost:$PORT/health/db || (echo "API health check failed" && exit 1)
          echo "API HEALTH OK"

      - name: Validate DB readiness (strict)
        run: |
          RESPONSE=$(curl -s http://localhost:$PORT/health/db)
          STATUS=$(echo "$RESPONSE" | jq -r '.status // "invalid"')

          if [[ "$STATUS" != "ok" ]]; then
            echo "DB not ready after boot"
            echo "$RESPONSE"
            exit 1
          fi
          echo "DB ready (status: ok)"

      - name: Run E2E
        run: |
          echo "E2E running..."

      - name: Debug logs
        if: failure()
        run: |
          echo "===== API LOG ====="
          cat apps/api/api.log 2>/dev/null || echo "No log file"

      - name: Cleanup
        if: always()
        run: |
          kill $(cat api.pid) || true