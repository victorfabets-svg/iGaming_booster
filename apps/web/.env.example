# Public API base URL. Empty → relative paths via Vite proxy (local dev).
# In production (Vercel), set to: https://<your-api-host>
VITE_API_URL=

# Dev-only JWT to call protected API endpoints from the web app.
# Generate via backend auth flow. NEVER commit a real token.
VITE_DEV_JWT=